# 1. 板侧环境配置
1. 安装anaconda3
2. 创建虚拟环境
3. 安装gcc等编译器

    ```sudo apt install build-essential```
4. 安装下述包：

    ```sudo dpkg -i packages/hailort_4.18.0_arm64.deb```

    ```sudo apt install --reinstall libkmod2 pciutils```
    
    ```pip install Denoising-rk3588J/packages/hailort-4.18.0-cp39-cp39-linux_aarch64.whl```
